@model HomeModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Bem vindo, @Model.nome</h1>

    <span>Seu e-mail para contato: @Model.email</span>
</div>
